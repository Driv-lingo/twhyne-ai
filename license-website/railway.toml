[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# IMPORTANT: Mount a persistent volume for the database
[[mounts]]
source = "license_data"
destination = "/app/data"
